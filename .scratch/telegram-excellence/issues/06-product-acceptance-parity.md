# Issue 06 — Product acceptance + iOS parity matrix

## Outcome

Demonstrable L2/L3 parity for Emanuel review.

## Acceptance (from plan)

1. Private DM link → `/link` → plain text turn → streaming bubble → file summary
2. `/stop` during run — with and without `turnId` in events
3. `/status` shows Mac identity, thread, branch, context (iOS status sheet fields)
4. Approve dangerous command from Telegram only
5. Voice note + photo → Codex on Mac
6. Reconnect bridge mid-turn — no duplicate final

## Deliverable

- [ ] Parity matrix in PR description (iOS feature → Telegram → N/A)
- [ ] Side-by-side task script: “fix tests, commit, push” on iOS vs Telegram
- [ ] Sign-off from Emanuel on “first-class” positioning

## Exit

Telegram not embarrassing next to iOS for the same remote task.
